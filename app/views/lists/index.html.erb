<h1>Lists of movies</h1>
<p>Choose the list you want.</p>

<%= link_to 'Create a new list', new_list_path, class: 'btn btn-success' %>
<ul>
<% @lists.each do |list| %>
  <li>
    <%= link_to list.name, list_path(list) %>
  </li>
<% end %>
</ul>
